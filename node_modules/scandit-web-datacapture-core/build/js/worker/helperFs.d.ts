/// <reference types="emscripten" />
interface SetupFSOptions {
    writableDataPathPreload: string;
    writableDataPath: string;
    preloading: boolean;
}
declare function setupFS(options: SetupFSOptions): Promise<void>;

export { setupFS };
